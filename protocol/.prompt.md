# Copilot Hints for Protocol code

- Use BLAKE3 utilities in `protocol.hashes` with the appropriate domain prefixes and `HASH_SEPARATOR`.
- Keep data classes deterministic and serializable via `canonical_json_bytes`; favor pure functions with clear validation.
- Error on invalid inputs with descriptive `ValueError` messages; maintain append-only semantics in ledger components.
