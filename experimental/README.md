# Olympus experiments

This directory is for non-production investigations. Nothing here is part of
the Olympus verifier contract, runtime API, release artifact set, or trust
model unless a later RFC/ADR explicitly promotes it.

Experiments must stay isolated from default builds and CI gates.
