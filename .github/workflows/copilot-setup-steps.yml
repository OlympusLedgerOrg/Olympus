name: Copilot setup steps

on:
  workflow_call:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Python is retired (v0.9.0): there is no pyproject.toml to
      # `pip install -e ".[dev]"`, so the former Python/venv/ruff setup
      # steps errored out (audit L-SC-3). Removed. The residual *.py tooling
      # is analyzed by CodeQL's python matrix entry, which needs no build.

      - name: Install Rust stable
        run: rustup toolchain install stable --no-self-update && rustup default stable

      - name: Set up Node.js 20
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.12.0"
          cache: "npm"
          cache-dependency-path: proofs/package-lock.json

      - name: Install proofs JS dependencies
        run: npm ci --ignore-scripts --prefix proofs

      - name: Cache powers of tau parameters
        id: cache-ptau
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: proofs/keys/powersOfTau28_hez_final_20.ptau
          key: ptau-20

      - name: Download powers of tau parameters
        if: steps.cache-ptau.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          mkdir -p proofs/keys
          PTAU_PATH="proofs/keys/powersOfTau28_hez_final_20.ptau"
          PTAU_URL="https://storage.googleapis.com/zkevm/ptau/powersOfTau28_hez_final_20.ptau"
          PTAU_B2="89a66eb5590a1c94e3f1ee0e72acf49b1669e050bb5f93c73b066b564dca4e0c7556a52b323178269d64af325d8fdddb33da3a27c34409b821de82aa2bf1a27b"

          if curl -fSL --connect-timeout 30 --retry 3 --retry-delay 5 \
            -o "${PTAU_PATH}" "${PTAU_URL}"; then
            echo "${PTAU_B2}  ${PTAU_PATH}" | b2sum --check -
          else
            echo "WARNING: Could not download PTAU parameters; skipping optional setup step."
            rm -f "${PTAU_PATH}"
          fi
