{
  "identifier": "default",
  "description": "Default capability for the Olympus main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default",
    "notification:default"
  ]
}
